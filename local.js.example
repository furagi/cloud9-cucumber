
var plugins = require("./default");

plugins.push("./cloud9.cucumber");
/*
for (var i = plugins.length - 1; i >= 0; i--) {
    if(typeof plugins[i].clientPlugins === 'Array') {
        plugins[i].clientPlugins.push("ext/cucumber/cucumber");
        break;
    }
};
*/
module.exports = plugins;
